import { useState } from "react";
import { useQA } from "../hooks";
type QATarget = "manager" | "lawyer";
export function QA({ onClose, onError, onPrivacy }: { onClose: () => void; onError: (message: string) => void; onPrivacy: () => void }) { const [target, setTarget] = useState<QATarget>("manager"); const [draft, setDraft] = useState(""); const [consent, setConsent] = useState(false); const { value, loading, create } = useQA(); const send = async () => { if (!consent) return; try { await create(target, draft); setDraft(""); } catch (cause) { onError(cause instanceof Error ? cause.message : "Не удалось отправить вопрос"); } }; const messages = (value ?? []).filter((item) => item.target === target); return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Вопрос-ответ" onMouseDown={onClose}><section className="qa-modal" onMouseDown={(event) => event.stopPropagation()}><header><div><h2>Вопрос-ответ</h2><p>переписка с агентством</p></div><button className="round-close" aria-label="Закрыть" onClick={onClose}>×</button></header><div className="qa-tabs"><button className={target === "manager" ? "current" : ""} onClick={() => setTarget("manager")}>Менеджер</button><button className={target === "lawyer" ? "current" : ""} onClick={() => setTarget("lawyer")}>Юрист</button></div><div className="qa-messages">{loading && <div className="comment-skeleton"><i /><i /></div>}{messages.map((message) => <div className="qa-message mine" key={message.id}><p>{message.body}</p><small>Вы · {message.status}</small>{message.answer && <div className="qa-message"><p>{message.answer}</p></div>}</div>)}</div><footer><textarea value={draft} onChange={(event) => setDraft(event.target.value)} placeholder={target === "manager" ? "Спросить менеджера…" : "Задать вопрос юристу…"} /><label><input type="checkbox" checked={consent} onChange={(event) => setConsent(event.target.checked)} /> Согласен с <button onClick={onPrivacy}>политикой обработки данных</button></label><button onClick={send} disabled={!draft.trim() || !consent}>Отпр.</button></footer></section></div>; }
