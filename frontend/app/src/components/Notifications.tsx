import type { Notification } from "../hooks";
type NotificationProps = { notifications: Notification[]; onReadAll: () => void };
const label = (item: Notification) => ({ qa_answered: "Менеджер ответил на ваш вопрос", review_approved: "Ваш отзыв одобрен", forum_message: "Новое сообщение в теме" }[item.type] ?? "Новое уведомление");
export function Notifications({ notifications, onReadAll }: NotificationProps) { return <section className="notification-popover"><header><strong>Уведомления</strong><button onClick={onReadAll}>Отметить всё прочитанным</button></header>{notifications.length ? <div>{notifications.map((notification) => <article key={notification.id}><i className={notification.is_read ? "read" : ""} /><div><p>{label(notification)}</p><small>{new Date(notification.created_at).toLocaleString("ru-RU")}</small></div></article>)}</div> : <p className="empty-notifications">Пока тихо</p>}</section>; }
